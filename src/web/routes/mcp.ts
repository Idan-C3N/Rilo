import type { FastifyInstance } from 'fastify';
import type { DB } from '../../db/db.js';

// TEMPORARY stub — fleshed out in Task 26.
export function registerMcpRoutes(_app: FastifyInstance, _db: DB): void {
  // no-op
}
